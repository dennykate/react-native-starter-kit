import { StyleSheet, Text, View } from "react-native";
import React from "react";
import Navbar from "../components/Navbar";

const Calculation = () => {
  return (
    <View>
      <Navbar title="တွက်ချက်ရန်" />
    </View>
  );
};

export default Calculation;

const styles = StyleSheet.create({});
